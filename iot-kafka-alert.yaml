apiVersion: apps/v1beta2 # for versions before 1.8.0 use apps/v1beta1
kind: Deployment
metadata:
  name: iot-kafka-alert
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iot-kafka-alert
  template:
    metadata:
      labels:
        app: iot-kafka-alert
        tier: backend
        group: java
    spec:
      containers:        
      - name: iot-kafka-alert
        image: iot-kafka-alert
        imagePullPolicy: IfNotPresent        
